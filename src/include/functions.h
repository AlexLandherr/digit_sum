#include <string>
#include <cstdint>

#ifndef FUNCTIONS_H
#define FUNCTIONS_H

namespace func {
    std::string add_num_str(std::string str1, std::string str2);
    uint64_t digit_sum(int exponent);
}

#endif